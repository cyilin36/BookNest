# 阅读器系统前端视觉风格与多端交互设计规范 (Visual Style Guide)

本文档基于 `reader-frontend-build.md` 规定的功能边界和 Naive UI 组件库约束编写，用于统一前后端分离架构下的前端 UI 视觉语言、排版审美、多端自适应交互规范以及主题管理。

---

## 1. 核心设计原则

*   **稳定清晰优先**：界面去装饰化，通过排版和空白（Negative Space）拉开信息层级，不干扰核心阅读。
*   **Naive UI 语境契合**：严格遵循 Naive UI 的“轻量、温和、高灵活性”视觉特色。
*   **引用模式视觉显化**：在“公共图书馆”等场景，通过视觉警示和状态标记（如：不占额外空间标签）让用户感知“引用添加”和“物理上传”的存储区别。
*   **极致自适应**：在移动端，菜单、目录、设置面板等复杂模块应完全向手机操作习惯倾斜。

---

## 2. 视觉主题规范 (Color Themes)

系统提供三种预设的视觉风格（对应前端 AppLayout 及阅读器 Typography 的 CSS 变量注入）：

### 2.1 简约现代 (Minimalist Modern) — Naive UI 经典绿 🌿
用于全站图书管理、后台设置、默认初始状态。
*   **主色 (Primary)**: `#18a058` (Naive UI 品牌绿)
*   **主色 Hover**: `#36ad6a`
*   **主色补色 (Suppl)**: `rgba(24, 160, 88, 0.1)`
*   **背景色 (Background)**:
    *   页面底色: `#f3f4f6` (Slate 100)
    *   卡片与侧栏: `#ffffff`
*   **文字颜色 (Text)**:
    *   主要文本: `#1f2937` (Gray 800)
    *   次要文本: `#6b7280` (Gray 500)
*   **边框颜色**: `#e5e7eb` (Gray 200)
*   **默认阅读字体**: `sans-serif` (Inter / system-ui)

### 2.2 温馨羊皮纸 (Cozy Sepia) — 实体书爱好者护眼 📜
专门适用于中长篇 TXT / EPUB 阅读场景，模拟纸张印刷感。
*   **主色 (Primary)**: `#8b5a2b` (暖木棕)
*   **背景色 (Background)**:
    *   页面底色: `#f4edd8` (经典复古黄)
    *   卡片与侧栏: `#fcf8ec`
*   **文字颜色 (Text)**:
    *   主要文本: `#2c2214` (炭焦黑)
    *   次要文本: `#6d583e` (中棕灰)
*   **边框颜色**: `#e3d5b5`
*   **默认阅读字体**: `serif` (Noto Serif SC / 优雅宋体)

### 2.3 深邃黑夜 (Deep Dark) — OLED 极度静谧 🌙
适用于深夜及微光环境下的沉浸阅读与极客模式。
*   **主色 (Primary)**: `#10b981` (翡翠绿 / 霓虹绿)
*   **背景色 (Background)**:
    *   页面底色: `#09090b` (真黑 OLED)
    *   卡片与侧栏: `#18181b` (Zinc 900)
*   **文字颜色 (Text)**:
    *   主要文本: `#f4f4f5` (Zinc 100)
    *   次要文本: `#a1a1aa` (Zinc 400)
*   **边框颜色**: `#27272a` (Zinc 800)
*   **默认阅读字体**: `serif` (Noto Serif SC)

---

## 3. 多端自适应与交互规范 (Responsive Rules)

| 屏幕断点 | 导航菜单实现 | 页面内边距 | 阅读器设置面板与目录 |
| :--- | :--- | :--- | :--- |
| **桌面端 (lg: $\ge 1024	ext{px}$)** | 侧边栏长驻展示 (`w-64`) | `p-8` (宽阔视界) | 左右分栏：左侧目录常驻，顶部工具栏常驻 |
| **手机与平板 (lg: $< 1024	ext{px}$)** | 隐藏侧边栏，启用顶部移动 Navbar，支持**侧滑或点击弹起导航抽屉 (Drawer Overlay)** | `p-4` (紧凑排版) | **底部滑出面板 (Bottom Sheet Drawer)**：点击目录/排版从底部滑出控制，滑动操作无缝承接。正文两翼边距自适应缩小 (`px-4`) |

---

## 4. 关键业务页面的视觉高标

### 4.1 我的书架 (BookshelfView)
*   **卡片设计**：每个书卡必须具有格式标签（TXT/EPUB/PDF，使用独特的色系，如 TXT 为琥珀色、EPUB 为绿松色、PDF 为粉红色）。
*   **进度显化**：书卡底部应有一个 1px 的灰色背景和主色填充的**进度条线（Linear Progress）**，直观显示阅读百分比和最后阅读时间。
*   **物理 vs 引用标签**：
    *   私有上传的图书：标记「私有上传」浅蓝徽章。
    *   引自公共图书馆的图书：标记「引自公共馆」紫色徽章。

### 4.2 公共图书馆 (LibraryView)
*   **存储教育 Banner**：页面顶部常驻一个淡绿色渐变 Banner，用温暖轻巧的语言警示和宣导：*“加入书架时仅在后端建立引用关系，共享同一物理文件，不占用您个人的存储空间配额。”*
*   **加入书架按钮状态**：
    *   未加入：展示主色高亮「加入书架」按钮。
    *   已加入：展示弱化灰色「已在书架」按钮（`disabled`）。

### 4.3 在线阅读器 (ReaderView)
*   **文字排布底线**：
    *   TXT/EPUB 必须使用 `text-justify`（两端对齐），避免屏幕右侧参差不齐。
    *   行高控制：提供 1.5倍（紧凑）、1.8倍（适中）、2.2倍（宽松）三档切换。
    *   字号控制：移动端默认 `16px`，桌面端默认 `18px`，步长为 `1px` 或 `2px` 连续微调。
*   **书签交互**：页面或章节段落旁边提供静默书签 Icon，点击即激活为黄色高亮，并伴随顶部 Toast 告知云端保存。

### 4.4 上传页面 (UploadView)
*   **拖拽区响应**：使用 2px 的虚线边框 (`border-dashed`)。鼠标拖入文件或点击时，虚线过渡为品牌主色。
*   **多目标指引卡片**：提供并列的“个人私有书架”与“全站公共图书馆”大选卡，通过大图标及清晰的权限描述（“仅自己可见”、“全站可引入”）辅助决策。

### 4.5 管理控制台 (AdminLayout)
*   **指标卡（Metrics）**：系统存储、待审核数、注册人数，需采用巨型黑体数字，并在右上角提供状态灯（如：待审核为橙色闪烁灯）。
*   **分类标签维护**：提供气泡式可删除 Chip，点击 `×` 有明显的过渡红变。

---

## 5. 前端验证参考网页
前端开发团队在编写具体 Vue/TS 组件前，可直接在浏览器中运行：
`/Users/cyilin/dev/book-server/frontend-style-demo.html`
这是一个基于本规范完全实现的 HTML/CSS 交互页面。

*   **测试方法**：
    1. 拖动浏览器窗口，检验宽屏（侧栏常驻）和窄屏（底部抽屉目录弹窗）的平滑过渡。
    2. 点击右上角的 3 套风格主题卡，验证全局主题变量（Sepia、Dark、Modern）的无缝过渡和排版字体的完美切换。
